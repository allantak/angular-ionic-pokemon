import { TypeBackGroundColorPipe } from './type-back-ground-color.pipe';

describe('TypeBackGroundColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeBackGroundColorPipe();
    expect(pipe).toBeTruthy();
  });
});
