import { StatsColorPipe } from './stats-color.pipe';

describe('StatsColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StatsColorPipe();
    expect(pipe).toBeTruthy();
  });
});
